interface:
  display_name: "SwiftUI App Craft"
  short_description: "Builds, reviews, and posts iOS SwiftUI code reviews with modern project-ready defaults."
  brand_color: "#0A84FF"
  default_prompt: "Use $swiftui-app-craft to build, review, post a review for, or refactor my iOS SwiftUI project."

policy:
  allow_implicit_invocation: true
